extends CanvasLayer

var bullet = get_parent().get_parent().get_node("")

func _ready():
	$layout/quantity.text =  "asdf"


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
